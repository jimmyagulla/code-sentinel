import { describe, it, expect } from 'vitest';

/**
 * Full @vscode/test-electron runs belong in a separate npm script / CI step.
 * Smoke test ensures the test harness is wired.
 */
describe('integration placeholder', () => {
  it('passes', () => {
    expect(true).toBe(true);
  });
});
